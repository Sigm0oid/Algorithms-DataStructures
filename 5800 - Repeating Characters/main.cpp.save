#include <bits/stdc++.h>

using namespace std;
typedef long long ll;

int main()
{
        int t;
        cin>>t;
        while(t--){
        ll nb_test,rep;
        string s,res="";
        cin>>nb_test>>rep>>s;

        for(ll i=0;i<s.length();i++){
            for(ll j=0;j<rep;j++)
            res+=string(s[i]);
        }
        cout<<nb_
        }
    return 0;
}
