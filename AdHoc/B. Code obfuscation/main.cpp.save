#include <bit/stdc++.h>

using namespace std;

int main()
{
    string s;
    cin>>s;
    int cmp=0;
    
    for(int i=0;i<n;i++){
    if((s[i]-'a')>cmp)
    {
        found=true;
        break;
    }
    }

    return 0;
}
