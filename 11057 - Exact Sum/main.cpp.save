#include <bits/stdc++.h>

using namespace std;
typedef long long ll;
typedef pair<ll,ll> ii;
ll n,sum;
vector<ll> prices;
void find_best(){

    for(ll i=0;i)

}

int main()
{
    while(cin>>n){
    prices.clear();
    for(int i=0;i<n;i++){
    ll x;
    cin>>x;
    prices.push_back(x);
    }
    cin>>sum;
    find_best();
    cout<<"Peter should buy books whose prices are "<<res.first<<" and "<<res.second<<"<<endl;
    }
    return 0;
}
