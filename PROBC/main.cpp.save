#include <bits/stdc++.h>
typedef long long ll ;
using namespace std;

int main()
{


    ll T ;

    scanf("%lld",&T) ;
    while (T--)
    {
		ll n , k ;

		scanf("%lld %lld",&n,&k) ;
        ll left=1,right=n;
        ll middle;
        do{
        middle=(left+right)/2;
        if((n-middle)*(n-middel)*(n-middle-1))<

        }while()

		printf("%lld %lld\n" ,x,y);
    }
}
