#include <bits/stdc++.h>
using namespace std ;
typedef long long ll ;
vector<ll> friends;
vector<ll> speeds;
int main()
{
    ll n;
    cin>>n;
    firends.resize(n);
    speeds.resize(n);
    for(ll  i=0;i<n;i++)
    cin>>friends[i];
    for(ll  i=0;i<n;i++)
    cin>>speeds[i];
    printf("%.6f\n",res);
    return 0;
}

